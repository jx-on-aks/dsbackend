{
  "name": "REPLACE_ME",
  "version": "1.0.0",
  "description": "Simple Hello World that listens on localhost:80",
  "main": "server.js",
  "dependencies": {
    "express": "^4.16.2"
  },
  "scripts": {
    "test": "echo \"no test specified\" && exit 0",
    "start": "node server.js"
  }
}
